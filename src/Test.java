

/*//Login method

public static void userChoice(){
        System.out.println("What do you want to do?");
        System.out.println("1 - Take a new Temperament test);
        // each user can take test several times, each entry should show a date

        System.out.println("2 - See results of my Temperament test);
        System.out.println("3 - Delete result of my Temperament test);
        System.out.println("4 - Log out);

        char optionChoice = scanner.nextLine().charAt(0);

        if (optionChoice == '1') {

        //DataBase class method for reading questions and recording results
        dataBase.Questions(topic,currentUserId);}

        } else if (optionChoice == '2') {
            if(dataBase.testIsDone(currentUserId,time)>0)
             {dataBase.readMyResults(currentUserId,time);
        }else{
        System.out.println("Test is not done yet");
        }
        }

        } else if (optionChoice == '3'){
        if(dataBase.testIsDone(currentUserId,time)>0)
        {dataBase.deleteMyResults(currentUserId,time);
        }else{
        System.out.println("Test is not done yet");}
        }

        // optionChoice == '4' = Logout method

*/




